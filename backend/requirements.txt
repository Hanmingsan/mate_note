# Core FastAPI and Server
fastapi==0.115.12
uvicorn[standard]==0.34.2 # Includes uvloop, httptools, websockets, watchfiles

# Data Validation and Settings
pydantic==2.11.3
pydantic-settings # For loading settings (version not specified in list, keep it flexible or add latest)
python-dotenv==1.1.0 # For loading .env files

# Database
sqlalchemy==2.0.40
aiosqlite==0.21.0 # Async SQLite driver

# Authentication & Security
passlib[bcrypt] # For password hashing (version usually inferred)
python-jose[cryptography]==3.3.0 # For JWT handling (Assuming standard version, list had cryptography==44.0.2)
python-multipart # For handling form data/file uploads (version usually inferred)

# OSS SDK
oss2==2.19.1 # Alibaba Cloud OSS SDK

# Other necessary dependencies often pulled in by the above:
# starlette==0.46.2 (FastAPI dependency)
# anyio==4.9.0 (FastAPI dependency)
# sniffio==1.3.1 (FastAPI dependency)
# typing_extensions (Used by Pydantic/FastAPI)
# click (Uvicorn dependency)
# h11 (Uvicorn dependency)
# httpx (Optional, useful for making HTTP requests from backend)
# cryptography (Pulled in by python-jose[cryptography])

# Note: The list provided contained many transitive dependencies.
# This requirements.txt focuses on the direct dependencies needed by the application code.
# pip will resolve the necessary sub-dependencies based on these.
